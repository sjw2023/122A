DROP DATAVERSE checkedtweets_test IF EXISTS;
CREATE DATAVERSE checkedtweets_test;
USE checkedtweets_test;

// Create the data type for our test dataset.
CREATE TYPE GenericType AS {
    _id: uuid
};

// Create the test dataset.
CREATE DATASET RawTweet(GenericType) PRIMARY KEY _id AUTOGENERATED;

// Load our test dataset. (Note: Replace "PATH_TO_HW8_FOLDER_GOES_HERE" !)
LOAD DATASET RawTweet USING localfs
    (("path"=":///Users/joowonsuh/hw8/data/test/rawtweet.json"),("format"="json"));

// Verify that our test dataset has been loaded correctly.
SELECT VALUE COUNT(*) FROM RawTweet;
